public class Ticket {
    private Long fee;

    public Long getFee() {
        return fee;
    }
}

// long → 기본 타입(primitive type)
// Long → 참조 타입(reference type), 클래스
//      - 객체 자체를 저장하는 게 아니라, 객체를 가리키는 값을 저장하는 타입
